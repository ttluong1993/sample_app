<%= form_for :relationships, url: user_relationships_path(user_id:
  current_user.id), remote: true do |f| %>
  <div><%= hidden_field_tag :followed_id, @user.id %></div>
  <%= f.submit "Follow", class: "btn btn-primary" %>
<% end %>
